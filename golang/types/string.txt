1. string: weighs 16: pointer the the first element of the underlying array, len

2. to calculate the length of runes utf8.RuneCountInString(str) or len([]rune()) or range

3.   
var builder strings.Builder

// Write strings to the builder
builder.WriteString("Hello, ")
builder.WriteString("World!")
builder.WriteString(" How are you?")

// Convert the builder content to a string
result := builder.String()